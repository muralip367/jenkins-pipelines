#!/usr/bin/env groovy

//@Grab('./choice_parameter.groovy')

    properties([
        parameters([
            choice(name: 'ENVIRONMENT',choices: ['DEV', 'QA', 'PROD'],description: 'Choose the target environment')
        ])
    ])
