#!/usr/bin/env groovy

//@Grab('./choice_parameter.groovy')

    properties([
        parameters([
            choice(name: 'environment', choices: JustTest().join('\n'), description: 'Choice')
        ])
    ])
